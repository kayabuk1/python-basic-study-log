# -*- coding: utf-8 -*-
"""
Created on Fri Jun 12 09:35:03 2026

@author: iot01
"""

#%% 10.3.5 委譲 delegate has-a関係
r'''◆前提知識
継承 ＝ 基底クラス と 派生クラスが密接 is-a 関係
リスコフの置換原則： 派生クラスのインスタンスは、常に基底クラスのインスタンスと置き換え可能。

'''
#↓リスコフ置換原則に違反している例
#後で書く

#↓委譲による解決をした例
class Mystack:
    #↓委譲先のオブジェクトに変数を保持
    def __init__(self):
        self.__data = []
        #↑インスタンス化されたと時に最初に作られるインスタンス変数__dataには
        #list型が入ることになる。
        
    #↓必要に応じて処理を委譲    
    def push(self, elem):
        self.__data.append(elem)
        #↑listに元々備わっているappendメソッドを使用して値を代入
        #ここが委譲の動きとのこと。
        #つまりlistオブジェクトを取り込む（生成して）、listオブジェクトの機能を使う。
        #●継承では、 class Mystack(list):としてしまう。→これでは何が良くないか？
    
    def pop(self):
        return self.__data.pop()
if __name__=="__main__":
    s = Mystack()
    s.push(40)
    print(s.pop())
    
r'''
●委譲のほうが使いやすそうだな。継承との使い分けは？
●pythonはオブジェクトやメソッド、関数同士を結びつける機能が色々ある気がする、、、。
    自分の中でうまく整理してまとめられていない。

A.第1部：なぜ class Mystack(list): は危険なのか？
スタック（Stack）というデータ構造は、**「最後に入れたものを、最初に取り出す
（LIFO：Last In, First Out）」**という絶対のルールを持っています。
だからこそ、操作は push（積む）と pop（取り出す）だけに制限されなければなりません。
もし、list クラスをそのまま継承（is-a関係）してしまったらどうなるでしょうか？
❌ 継承した場合の悲劇（リスコフ置換原則の崩壊）
class MyStack(list):
    def push(self, elem):
        self.append(elem)
s = MyStack()
s.push(10)
s.push(20)

# 【悲劇】listを継承しているため、親の全機能が使えてしまう！
s.insert(0, 99) # スタックの底に勝手に割り込める
s.remove(10)    # 途中のデータを勝手に消せる
s.sort()        # 順番を勝手に並び替えられる

現場監督：「おいおい！ スタックというのは上からしか出し入れできないルールのはずなのに、
        list の親方から借りてきた insert や sort 工具を使って、誰でも横から
        データをいじれてしまうじゃないか！ これじゃあスタックの前提が崩壊してシステムが
        狂うぞ！」
**「リスコフの置換原則（派生クラスは基底クラスと置き換え可能でなければならない）」**の
観点から見ても、「スタックはリストの一種（is-a）である」と言うのは無理があります。
リストが持つ「どこからでも出し入れ自由」という性質をスタックは満たせないからです
。
⭕ 委譲（has-a）による解決 
、「自分自身の金庫（self.__data）の中に list を部品として隠し持ち、
外部から使える工具は push と pop だけに制限する（カプセル化）」持っている
（has-a）」関係であり、これが委譲の真理です。

第2部：継承（is-a）と委譲（has-a）の使い分け
実務において、この2つは以下のように使い分けます。 
プロの世界では、結合度が強すぎてバグの温床になりやすい継承よりも、
**「まずは委譲（コンポジション）を優先して検討せよ」**という格言があるほどです。
⚔️ 継承（is-a関係: 〜は〜の一種である）
基準: 親クラスの性質やメソッドを**「100%すべて」**引き継いでも全く問題がない場合。
インフラの例: class CiscoRouter(BaseRouter):
Ciscoルーターは、ルーターの一種である（is-a）。
親の「IP設定機能」「再起動機能」などをすべてそのまま使えるため、継承が適している。
⚔️ 委譲（has-a関係: 〜は〜を持っている）
基準: 別のクラスの機能のうち、**「一部の便利な機能だけをつまみ食いしたい
」「内部の仕組みとしてこっそり使いたい」**場合。
インフラの例: class NetworkDevice: の中に self.ssh = SSHConnection() 
を持たせる。
ネットワーク機器は、SSH接続そのもの（is-a）ではありません
。「SSH接続という部品を持っている（has-a）」だけです。
だから継承ではなく、初期化時に部品として内部に保持（委譲）させ、
必要な時だけ self.ssh.send_command() のように仕事を任せる（委譲する）のです。

★第3部：【総括】 Pythonの「結びつき機能」完全マップ
これまで学んできた「オブジェクト同士を結びつける機能」は、一見バラバラに見えますが、
アーキテクチャの目的別に整理すると非常に美しいマトリクスになります
。
① クラスの「縦」の結びつき（家系図の構築）
 継承（Inheritance）: 親の設計図を丸ごと引き継ぐ。super() を使って
                  親の処理を呼び出し、差分だけを追加する。
 抽象基底クラス（ABC）: ポリモーフィズム。親が子に対して「この名前のメソッドを絶対作れ！」
                 と契約を強制する。
② クラスの「横」の結びつき（機能の合成）
 ミックスイン（Mixin）: メインの家系図（継承）とは別に、ログ出力などの「単独の
               機能」だけを多重継承で差し込む。
 委譲（Delegate / has-a）: 別のクラスのインスタンスを自分の変数の中に
                　　　　 「部品」として持ち、特定の仕事をその部品にやらせる。
③ 関数の「動的」な結びつき（ラッパー）
 高階関数 / クロージャ: 関数を別の関数に引数として渡し、状態を保持したまま
                   後で実行する。
 デコレータ（@）: 既存の関数を「改造工場」に入れ、中身をいじらずに前後に新しい機能
           （ログ出力や認証など）を被せて出力する。
④ データの「型」に縛られない結びつき
 ダックタイピング: 継承関係が全くなくても、同じ名前のメソッド（例：get_facts()）
            を持っていれば、現場監督は気にせず同じように実行してくれる。

■ まとめ
Pythonのアーキテクチャは非常に柔軟ですが、設計の基本方針は以下のようになります。
「親の全機能を使わせたいか？」 ➔ YESなら「継承」、 NOなら「委譲」。
「複数のクラスで共通の機能だけを使い回したいか？」 ➔ 「Mixin」または「デコレータ」。
「同じ命令で複数の異なる機器を操作したいか？」 ➔ 「ポリモーフィズム（ダックタイピングやABC）」。
'''
#%%10.3.6　ミックスインMixin 直接利用しない継承目的のメソッド束ねた断片的なクラス
#書き方自体は普通のクラス構文と変わらない。
#多重継承時に、基底クラス継承は1つのみとし、他は機能だけミックスインにすることで複雑化防止
class LogMixin:
    #現在のインスタンスの内容を列挙する機能のメソッド
    #↓__init__も書かず、インスタンス変数が定義されていないので、
    #それ単体ではshow_attrメソッドは意味を成さない。
    #●↑これはオブジェクトが生成されないので、selfに何も突っ込めないということ？？
    #A.Pythonでは __init__ メソッドがなくてもオブジェクト自体は生成できる。
    def show_attr(self):#attr=アトビュートのこと
        for key, value, in self.__dict__.items():
            # ↑ 辞書型のメソッドであるitem"s"()を使う
            print(f'{key}:{value}')
            
#ミックスインクラスを組み込み
class Person(LogMixin):
    def __init__(self,name,age):
        self.name = name
        self.age = age
if __name__=="__main__":
    p = Person('山田太郎', 50)
    p.show_attr()
    # name:山田太郎
    # age:50
    mix = LogMixin()
    #↑オブジェクトの生成自体は出来る。
    print(mix.show_attr())
    #None しかし何も表示されない。
    print(mix.__dict__)
    #{}空の辞書が表示される
r'''
●↑これはオブジェクトが生成されないので、selfに何も突っ込めないということ？？
A.Pythonでは __init__ メソッドがなくてもオブジェクト自体は生成できる
ではなぜ「それ単体では意味を成さない」のか？ 
それは、**「ミックスイン単体でオブジェクトを作っても金庫（__dict__）が空っぽだから」
であり、同時に「ミックスインのメソッドが要求する self とは、
将来この部品を合体させる『別の誰か』の実体を想定しているから」**です。

第1部：__init__ がなくてもオブジェクトは生まれる
公式ドキュメントに「インスタンス化操作では、空のオブジェクトが作られます。
[...] 特定の初期状態にカスタマイズされたオブジェクトを作りたい場合には
 __init__() を定義できます」とあるように、__init__ はあくまで「
 オブジェクト誕生直後のデータ投入処理」に過ぎません
。
したがって、以下のようにミックスイン単体で実体を作ることはシステム上可能です。
lm = LogMixin() # エラーにはならない！ 空の実体が作られる。
lm.show_attr()  # 実行できるが、何も出力されない。
なぜ単体だと意味を成さないのか？
 lm という実体は生まれましたが、__init__ による変数の登録がないため、
 個人の金庫である lm.__dict__ は完全に「空っぽ」です。
 そのため、show_attr() を実行して self.__dict__.items() を回しても、
 何も表示されません。これが「単体では意味を成さない」の正体です。
 
第2部：Mixinとは「他人の金庫を開けるアドオン工具」である
ミックスインの真の価値は 「データを持っている他のクラス（Person など）に、
多重継承を使って機能（メソッド）だけを後付けすること」 にあります。
このとき、ミックスイン側の show_attr(self) に書かれている self とは
何を指しているのでしょうか？ 
これこそがPythonの「ダックタイピング（動的バインディング）」の極致です。
LogMixin の設計図に書かれた self は、「自分自身の実体」ではなく
、「将来、自分を継承してくれるであろう『呼び出し元の実体（今回は p）』」 を指しているのです
。
■ 現場監督のスローモーション（ p.show_attr() 実行時）
現場監督（誕生）：「Person の実体 p を作れ！ Person の __init__ を起動して、
            実体 p の金庫（p.__dict__）に name: '山田太郎' と 
            age: 50 を格納だ！」
現場監督（検索）：「よし、p.show_attr() を実行しろ！ 個人の金庫にはない。
            金型 Person の辞書にもない。よし、多重継承の MRO に従って
            親の LogMixin の辞書を探せ……おっ、工具（show_attr）を発見！」
現場監督（魔法の発動）：「LogMixin から借りてきた show_attr(self) の self 
                    には、呼び出し元である実体 p を代入して実行しろ！」
現場監督（出力）：「命令は self.__dict__.items() だな。
            今の self は p だから、開けるのは LogMixin の金庫ではなく、
            実体 p の金庫だ！ よし、中に入っている name: 山田太郎 と 
            age: 50 を出力だ！」

第3部：インフラアーキテクチャにおけるMixinの価値
 「多重継承時に、基底クラス継承は1つのみとし、他は機能だけのミックスインにすることで
 複雑化防止」 というのは、プロのインフラエンジニアが厳守するアーキテクチャ設計の鉄則です。
たとえば、ネットワーク自動化システムを作る際、「Ciscoルータークラス」と「Juniperルータークラス」が
あるとします。 両方の機器に対して「操作ログをファイルに書き出す機能」を追加したい場合、
それぞれのクラスに同じコードを書くのはDRY原則違反です。
かといって、共通の親クラスである「ルータークラス」にログ出力機能を混ぜてしまうと、
ルーターの本質的な機能とログ管理の機能が密結合してしまい、設計図がカオスになります。
そこで、今回のような LogMixin（ログ出力専用の後付け部品）を作ります。
class CiscoRouter(Router, LogMixin):
    pass

class JuniperRouter(Router, LogMixin):
    pass
このように設計することで、
メインの継承ツリー（ルーターとしての家系図）はシンプルに保つ。
「特定の機能（ログ出力など）」だけを、USBポートにガジェットを挿すかのように後付け
（プラグイン）できる。
■まとめ
●↑これはオブジェクトが生成されないので、selfに何も突っ込めないということ？？
【結論】: オブジェクトは生成できます。しかし、Mixinクラスは自分の実体（self）を作ることを
目的としておらず、「他のクラス（Person）の実体に合体（継承）し、合体先の実体（self）を
操作するための『汎用的なアドオン部品』」 として設計されているため、
単体で呼び出しても（self の金庫が空なので）意味をなさないのです。
'''

#%%# 10.4 ポリモーフィズム Polymorphism 多態性
#→つまり、同じ名前のメソッドで異なる挙動を実現すること。
#●オーバーロードとの使い分けは？違いは？

# 10.4.1 ポリモーフィズムの基本
class Figure():
    #width,heightを準備
    def __init__(self,width,height):
        self.widht = width
        self.height = height
    #面積を取得（中身はダミー）
    def get_area(self):
        return 0.0

class Triangle(Figure):
    #三角形の面積を求めるget_areaメソッドを定義
    def get_area(self):
        return self.widht*self.height/2

class Rectangle(Figure):
    #四角形の面積を求めるメソッドを定義
    def get_area(self):
        return self.widht*self.height

if __name__=="__main__":
    t = Triangle(10, 15)
    r = Rectangle(10, 15)
    print(t.get_area()) #75.0
    print(r.get_area()) #150
    f = Figure(10,15) #←一応インスタンス化するこは出来る
    print(f.get_area()) #0.0
    
R'''●オーバーロードとの使い分けは？違いは？
第1部：【解剖】 ポリモーフィズムとオーバーロードの決定的な違い
結論から言うと、両者は「同じ名前のメソッドを使う」という点は似ていますが、**「誰に対して命令を送るか」**が全く異なります。
⚔️ オーバーロード（多重定義）
対象: **「同じ相手（同じクラス）」**に対して。
仕組み: 渡す荷物（引数の数や型）を変えることで、相手に違う処理（同じ名前の別メソッド）を実行させる。
※Pythonでは引数の数での多重定義はできず、デフォルト引数や @singledispatch で代用すると学びましたね。
⚔️ ポリモーフィズム（多態性）
対象: **「違う相手（違うクラス）」**に対して。
仕組み: 同じ名前、同じ荷物（引数）のメソッドを実行させるだけで、相手が勝手に「自分専用の処理」を行って結果を返してくる。
※今回のコードがまさにこれです。
■ 現場監督のスローモーション（今回のコードの裏側）
今回のコードで現場監督は、相手が三角形（t）だろうが四角形（r）だろうが、**「図形（Figure）の仲間なら get_area という工具を絶対に持っているはずだ！」**と信じて、中身を気にせずに同じ命令を下しています。
現場監督：「おい、そこの三角形 t！ 面積を出せ！ t.get_area() 実行！」 ➔ 現場監督は Triangle の辞書から get_area を見つけ、底辺×高さ/2 の処理を実行（出力: 75.0）。
現場監督：「次は四角形 r！ お前も面積を出せ！ r.get_area() 実行！」 ➔ 現場監督は Rectangle の辞書から get_area を見つけ、底辺×高さ の処理を実行（出力: 150）。
「呼び出す側（現場監督）は、相手の種類ごとにif文で条件分岐をしなくてよい。ただ一律に get_area() と命令するだけで、相手が勝手に適切な計算方法（オーバーライドしたメソッド）を選んでくれる」。これがポリモーフィズムの正体です。
第2部：【実務】 インフラ自動化における究極のポリモーフィズム（NAPALM）
このポリモーフィズムが、インフラエンジニアの業務でどのように役立つのか？ 実は昌敏さんが学ぶ予定のネットワーク自動化ライブラリ NAPALM こそが、このポリモーフィズムの概念を極限まで利用したフレームワークなのです
。
インフラ環境には、CiscoのルーターやJuniperのルーターなど、ベンダーの違う機器が混在しています。もしポリモーフィズムを使わずに情報を取得しようとすると、自動化スクリプトは最悪の設計（密結合）になります。
❌ ポリモーフィズムを使わない最悪のスクリプト（if文の嵐）
for router in routers:
    if isinstance(router, CiscoRouter):    # Ciscoだったら
        router.run_show_version()          # Cisco専用のメソッドを呼ぶ
    elif isinstance(router, JuniperRouter):# Juniperだったら
        router.execute_show_system()       # Juniper専用のメソッドを呼ぶ
⭕ ポリモーフィズムを使ったNAPALMの設計（疎結合）
# routerがCiscoのインスタンスだろうと、Juniperのインスタンスだろうと気にしない！
for router in routers:
    facts = router.get_facts() # 全ベンダー共通のメソッド名（ポリモーフィズム）
    print(facts)
NAPALMの内部では、CiscoDriver クラスも JuniperDriver クラスも、大元となる基底クラス（NetworkDriver）を継承しており、それぞれが get_facts() というメソッドを独自にオーバーライドして実装しています
。 そのため、自動化スクリプトを書く私たち（呼び出し元）は、相手のベンダーをいちいちif文で判定する必要がなく、ただ一律に router.get_facts() と命令するだけで良いのです。
■ 参謀からのまとめ
【オーバーロード】
**「同じルーター」**に対して、set_ip(文字列) を渡すか、set_ip(リスト) を渡すかで内部処理を分岐させる技術。
【ポリモーフィズム】
「CiscoとJuniper」共通のコマンドを無差別に叩き込み、それぞれの機器が勝手に適切なコマンド（show ip int brief や show interfaces terse）を実行して返してくるようにするアーキテクチャ設計。
'''

#%% 10.4．2 抽象メソッド @abstractmethod 
#抽象メソッドを継承したクラスは、 インスタンス化したければ全抽象メソッドをオーバライドする義務
from abc import abstractmethod, ABC
#↑ABC＝Abstract Base Classの略
#↓抽象クラスの名前のサフィックス接尾辞には抽象クラスとわかるような単語を付けるのが良い。
class FigureAbstract(ABC):
    def __init__(self,width,height):
        self.widht = width
        self.height = height
    @abstractmethod
    def get_area(self):
        pass
    
class Triangle(FigureAbstract):
    #三角形の面積を求めるget_areaメソッドを定義
    def get_area(self):
        return self.widht*self.height/2

class Rectangle(FigureAbstract):
    #四角形の面積を求めるメソッドを定義
    def get_area(self):
        return self.widht*self.height
#↓テスト１：オーバライドしないと
# class Rectangle(FigureAbstract):
#     def get_area(self):
#         pass
#     #～中略～
#     #print(r.get_area())
#     #None一応抽象元と同じ記述でもオーバライドしたことになる？
#↓テスト2：オーバライドしないと
# class Rectangle(FigureAbstract):
#     pass
#     #～中略～
    #TypeError: Can't instantiate abstract class Rectangle
    #with abstract method get_area

if __name__=="__main__":
    t = Triangle(10, 15)
    r = Rectangle(10, 15)
    print(t.get_area()) #75.0
    print(r.get_area()) #150
    f = FigureAbstract(10,15) #
    #TypeError: Can't instantiate abstract class FigureAbstract 
    #with abstract method get_area

r'''
第1部：【真理】 抽象基底クラス（ABC）の正体は「絶対の契約書」
公式ドキュメントにおいて、抽象基底クラス（ABC）は**「インターフェースを定義する方法を提供するもの」とされています
。インターフェースとは、「このクラスを継承するなら、絶対にこの名前のメソッド（工具）を作れ！」というシステムとの契約**です。
通常の継承では、子クラスがメソッドを作り忘れても、現場監督は「まあ、親の辞書から借りてくればいいか」と自動で親のメソッドを使ってしまいます。しかし、図形の面積（get_area）のように、**「三角形と四角形では計算式が全く違うから、親のダミー処理を勝手に使われては困る（必ず子クラス専用の処理を作らせたい）」**というケースが多々あります。
ここで @abstractmethod というデコレータ（魔法）の出番です。 親クラスのメソッドにこの魔法をかけると、現場監督の思考が「親のものを貸してやる」から**「子クラス自身が絶対に独自の工具を作らなければ、実体の製造（インスタンス化）を一切許可しない」**という厳格なエラーチェックモードに切り替わるのです。
第2部：【解剖】 現場監督のスローモーション（検証コードの裏側）
昌敏さんの検証コード（Test 1, Test 2, 抽象クラス自身のインスタンス化）で、現場監督がどう動いたのかをスローモーションで追います。
■ 抽象クラス自身のインスタンス化（ f = FigureAbstract(10,15) ）
現場監督：「FigureAbstract の実体 f を作れだな！ ……おい待て、この金型には @abstractmethod という『未完成の印』が貼られているぞ！」
現場監督：「この金型はあくまで『子クラスに契約を強制するための概念的な設計図』だ！ こんな未完成の設計図から直接実体を作ることは許さん！ 強制終了だ！」 ➔ （出力）TypeError: Can't instantiate abstract class FigureAbstract...
■ Test 2: オーバーライドしないと（ class Rectangle(FigureAbstract): pass ）
現場監督：「Rectangle の実体 r を作れだな！ まずは親との『契約』を満たしているかチェックするぞ！」
現場監督：「親の FigureAbstract は、『必ず get_area を作れ』と契約を要求している。しかし Rectangle の辞書には get_area が無い（pass しか書かれていない）じゃないか！ 契約違反だ！ 実体の製造を中止する！」 ➔ （出力）TypeError: Can't instantiate abstract class Rectangle with abstract method get_area
■ Test 1: pass でオーバーライドした場合の疑問について
None一応抽象元と同じ記述でもオーバライドしたことになる？
➔ 【結論】はい、完全なオーバーライドとみなされます！
現場監督：「Rectangle の実体を作るぞ！ 親の契約である get_area は子クラスの辞書にあるか？ ……おっ、ちゃんと def get_area(self): として登録されているな！ よし、中身は知らんが契約は満たした！ 実体 r の製造を許可する！」
現場監督（実行時）：「r.get_area() を実行しろ！ 中身は pass（何もしない）だな！ Pythonの絶対ルールとして、return がない関数（プロシジャ）は強制的に None を返すから、None を出力だ！」
Pythonの現場監督がチェックするのは**「辞書の中にその名前のメソッド（名札）が存在するかどうか」**だけです。中身が pass であろうと、立派にオーバーライドされたと判定され、エラーにはなりません。
第3部：【実務】 なぜこの「義務」がインフラ設計で最強なのか？
この「インスタンス化したければ全抽象メソッドをオーバライドする義務」、実務（特に複数人でのシステム開発や自動化フレームワーク）において**「フェイルファースト（Fail Fast：問題があれば可能な限り早く落とす）」**という最強の防御力をもたらします。
たとえば、ネットワーク自動化のNAPALMのようなシステムを作るとします。 親クラスとして BaseRouter（ABC）を作り、@abstractmethod で get_interfaces() を定義します。
もし新しいメンバーが YamahaRouter(BaseRouter) クラスを作った際、うっかり get_interfaces メソッドを書き忘れたとします。 ABCを使っていなければ、スクリプトを実行して、**いざ本番環境のルーターに接続し、情報を取得しようとした瞬間に「そんなメソッドはない」とクラッシュ（本番障害）**してしまいます。
しかしABCを使っていれば、「スクリプトが起動し、YamahaRouter のインスタンスを生成しようとした最初の0.1秒の瞬間」に TypeError で落ちてくれます。 被害がシステム全体に波及する前に、開発者に「おい、メソッドを書き忘れてるぞ！」と強烈に警告してくれるのです。
■ 参謀からのまとめ
ABC（抽象基底クラス）: abc モジュールを使うことで、「このクラスはインスタンス化できない」「継承先には特定のメソッドの実装を強制する」という「インターフェース（契約）」を定義できる
。
エラーの発生タイミング: メソッドを呼び出した時ではなく、**「インスタンスを作ろうとした瞬間（ r = Rectangle() ）」**にチェックが走り、不足があれば TypeError で安全に実行を阻止する。
オーバーライドの判定基準: メソッドの「名前」が子クラスの辞書に登録されていれば契約履行とみなされるため、中身が pass でも構文上はエラーにならない。
'''

#%% 10.4.3 instance関数 オブジェクトが実装している抽象クラスを確認できる関数
# instance(objチェックしたいインスタンス, clazz任意のクラス)
from abc import abstractmethod, ABC
class FigureAbstract(ABC):
    def __init__(self,width,height):
        self.widht = width
        self.height = height
    @abstractmethod
    def get_area(self):
        pass
    
class Triangle(FigureAbstract):
    #三角形の面積を求めるget_areaメソッドを定義
    def get_area(self):
        return self.widht*self.height/2

class Rectangle(FigureAbstract):
    #四角形の面積を求めるメソッドを定義
    def get_area(self):
        return self.widht*self.height

if __name__=="__main__":
    #figureAbstract派生クラスのリストを準備
    #●とは？？派生クラスのインスタンスオブジェクトを生成してリスト化ということ？
    figs = [
        Triangle(10, 15),
        Rectangle(10, 15),
        Triangle(5, 1)
    ]
    #配列figsの内容を順番に処理
    for fig in figs:
        if isinstance(fig, FigureAbstract):
        #↑継承している場合、Trueが帰ってくる。
            print(f'{fig.__class__}:{fig.get_area()}')
            #<class '__main__.Triangle'>:75.0
            #<class '__main__.Rectangle'>:150
            #<class '__main__.Triangle'>:2.5
    for fig in figs:
        if issubclass(fig.__class__, FigureAbstract):
        #↑継承している場合、Trueが帰ってくる。
            print(f'{fig.__class__}:{fig.get_area()}')
            #<class '__main__.Triangle'>:75.0
            #<class '__main__.Rectangle'>:150
            #<class '__main__.Triangle'>:2.5 結果は同じ
r'''●issubclass関数との使い分けは？
第1部：【回答】 派生クラスのリスト化についての疑問
#●とは？？派生クラスのインスタンスオブジェクトを生成してリスト化ということ？
大正解です！ まさしくおっしゃる通りです。 Triangle(10, 15) のようにカッコ () を付けてクラスの「実体（インスタンス）」を生成し、それらを [] で囲んで1つのリスト（コンテナ）にパッキングしています
。
インフラ自動化で例えるなら、「Ciscoルーターの実体」と「Juniperルーターの実体」をそれぞれ生成し、target_routers = [cisco_rtr, juniper_rtr] のように1つのリストにまとめて、後で for ループで一気に処理するための準備をしている状態です。
第2部：【解剖】 isinstance と issubclass の根本的な違い
公式ドキュメントには、この2つの組み込み関数の違いが明確に定義されています
。
isinstance(obj, class)： **「実体（インスタンス）」**が、特定のクラス（またはその親クラス）から作られたものかを判定する
。
issubclass(subclass, class)： **「金型（クラス）」**同士の継承関係を判定する
。
この根本的な違いを、現場監督のスローモーションで追ってみましょう。
■ 現場監督のスローモーション（ isinstance の場合）
if isinstance(fig, FigureAbstract):
現場監督：「変数 fig に入っている 『実体』 は、FigureAbstract という金型、あるいはその子孫の金型から作られたものか？」
現場監督：「おっ、この実体は Triangle クラスから作られていて、Triangle の親は FigureAbstract だな！ よし、身内だと確認できたから True だ！」
■ 現場監督のスローモーション（ issubclass の場合）
if issubclass(fig.__class__, FigureAbstract):
現場監督：「おいおい、issubclass には『実体』をそのまま渡してはいけないルールだぞ！ だからわざわざ fig.__class__ として、『この実体を作った金型（Triangleクラス）』 を引っ張り出して渡してきているな！」
現場監督：「よし、判定しよう。『Triangle 金型』は、『FigureAbstract 金型』のサブクラス（子孫）か？ ……うん、継承しているな！ だから True だ！」
結果としてどちらも同じ判定になりますが、issubclass は「クラスの設計図同士の家系図」を調べるためのツールなので、実体（fig）に対して使うためにわざわざ __class__ を呼び出して金型を渡すという「回り道」をしていることが分かります。
第3部：【実務】 インフラアーキテクトの使い分け（ベストプラクティス）
では、実務においてこの2つをどう使い分けるのか？ 結論から言います。
【結論】 手元に 「実体（インスタンス）」 がある場合は、絶対に isinstance() を使います。 手元に 「クラス名（金型の名前）」 しかない場合に限り、issubclass() を使います。
■ isinstance の実務ユースケース（99%はこちら） 今回のように for fig in figs: でループを回している時、手元にあるのは作られた後の「実体」です。この実体が「本当に get_area() を持っている図形の仲間かどうか（契約を満たしているか）」をチェックして安全にメソッドを実行するために isinstance() を使います。
■ issubclass の実務ユースケース（特殊なケース） インフラ自動化のフレームワークなどで、「まだ実体は作っていないけれど、ユーザーから『このクラスを使ってルーターを作ってね』と金型の名前だけを渡された時」に使います。
# ※まだインスタンス化していない！ クラス名（金型）だけが渡された状態
def create_router(router_class):
    # 実体を作る前に、渡された金型が本当に BaseRouter の子孫かチェックする！
    if issubclass(router_class, BaseRouter):
        return router_class('192.168.1.1') # ここで初めてインスタンス化する
    else:
        raise TypeError("ルーターのクラスではありません！")
■ 参謀からのまとめ
isinstance: **「実体」**に対して、あなたが属している家系（型）は何ですか？と尋ねる関数。
issubclass: **「金型（クラス）」**に対して、あの金型はあなたの祖先ですか？と尋ねる関数。
ポリモーフィズムにおける防弾装甲: リストの中にどんな正体不明のオブジェクトが混ざっていても、if isinstance(obj, 親クラス): で「契約を満たしている身内」だけをフィルタリングすれば、存在しないメソッドを呼んでシステムがクラッシュするのを100%防ぐことができる。
'''






















