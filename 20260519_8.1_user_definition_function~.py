# -*- coding: utf-8 -*-
"""
Created on Tue May 19 14:42:32 2026

@author: iot01
"""

#%%## chapter 8 ユーザー定義関数
## 8.1 ユーザー定義関数の基本
# 8.1.1 ユーザー定義関数の基本構文 def命令

#get_triangle関数を定義
def get_triangle(base,height):
    return base*height/2
#get_triangle関数を呼び出す
area = get_triangle(8, 10)
print(F'三角形の面積は{area:.5}です。')
    #三角形の面積は40.0です。
print(type(area))
print(get_triangle)
print(type(get_triangle))
print(dir(type(get_triangle)))
print(dir(get_triangle))
#<class 'float'>
#<function get_triangle at 0x0000021E7F5F0400>
#<class 'function'>
'''
['__annotations__', '__builtins__', '__call__', '__class__', '__closure__', '__code__', '__defaults__', '__delattr__', '__dict__', '__dir__', '__doc__', '__eq__', '__format__', '__ge__', '__get__', '__getattribute__', '__getstate__', '__globals__', '__gt__', '__hash__', '__init__', '__init_subclass__', '__kwdefaults__', '__le__', '__lt__', '__module__', '__name__', '__ne__', '__new__', '__qualname__', '__reduce__', '__reduce_ex__', '__repr__', '__setattr__', '__sizeof__', '__str__', '__subclasshook__']

['__annotations__', '__builtins__', '__call__', '__class__', '__closure__', '__code__', '__defaults__', '__delattr__', '__dict__', '__dir__', '__doc__', '__eq__', '__format__', '__ge__', '__get__', '__getattribute__', '__getstate__', '__globals__', '__gt__', '__hash__', '__init__', '__init_subclass__', '__kwdefaults__', '__le__', '__lt__', '__module__', '__name__', '__ne__', '__new__', '__qualname__', '__reduce__', '__reduce_ex__', '__repr__', '__setattr__', '__sizeof__', '__str__', '__subclasshook__']
'''
import types
# types道具箱の中にある 'FunctionType' という名前の設計図を取り出す
print(dir(types.FunctionType))


#%% 8.1.4 戻り値 return命令
def show_current():
    import datetime
    print(datetime.datetime.now())
print(show_current())
print(show_current)
print(type(show_current))
print(type(show_current()))
#2026-05-19 15:14:22.172894
#None
#<function show_current at 0x0000021E7F5F16C0>
#<class 'function'>
#2026-05-19 15:18:05.700817
#<class 'NoneType'>

import datetime
print(datetime)
print(type(datetime))
print(type(datetime.datetime))
print(datetime.datetime)
print(type(datetime.datetime.now))
print(datetime.datetime.now)
print(dir(datetime.datetime))
print(dir(datetime.datetime.)
'''
<module 'datetime' from 'C:\\ProgramData\\spyder-6\\envs\\spyder-runtime\\Lib\\datetime.py'>
<class 'module'>
<class 'type'>
<class 'datetime.datetime'>
<class 'builtin_function_or_method'>
<built-in method now of type object at 0x00007FFEC27F2FC0>
'''

#%%　練習問題5
'''
次の各関数について、定義の1行目に記述する内容と、戻り値がある場合は望ましいデータ型を答えよ。なお、データ型は「int、float、str、bool」の4つの型、リスト・ディクショナリ・タプル・セットから選びなさい。
1呼び出すと、「今日は晴れです」という文字列を画面に表示するweather関数
2円の半径を渡すとその円の面積を返す calc_circle_are関数
3呼び出すと現在時刻を調べて、「18時25分30秒」のようなデータを返す nowstr関数
4呼び出しと現在時刻を調べて、時分秒を表す３つの数値を返すnowint関数
5西暦を渡すと、うるう年かを判定するis_leapyear関数

'''
# 5-1
#1.def weather():
#2.def calc_circle_area(radius): , float
#3.def nowstr(): , str
#4.def nowint(): , tuple
#5.def is_leapyear(year): , bool

#%%　練習5－2
'''
練習5－1の「5」is_leapyer関数を、以下の判定方法を参考にして関数定義を完成させなさい。
うるう年の判定方法
・400で割り切れる年はうるう年
・４で割り切れる年はうるう年だが、１００で割り切れる年はうるう年ではない
また、キーボードから現在の西暦を入力させてこの関数を呼び出して、次のように表示するプログラムを作成してください。
・うるう年だった場合　　　：西暦～～年は、うるう年です
・うるう年ではなかった場合：西暦～～年は、うるう年ではありません

'''
year = int(input('現在の西暦を入力してください:'))
def is_leapyear(year):
    if( year % 400 == 0):
        return True
    elif(year % 4 == 0):
        if(year % 100 == 0):
            return False
        else:
            return True
    else:
        return False
    #どれにも当てはまらないでreturnせず終了した関数はNoneを返すので
    #下のif分が実行されずにコンソールには何も表示されなくなってしまう。
    #なので例外処理のelse:が必要。
if (is_leapyear(year)==True):
    print(F'西暦{year}年は、うるう年です')
elif(is_leapyear(year)==False):
    print(F'西暦{year}年は、うるう年ではありません')
#テスト実行数値： 
#2000　※400*5
#現在の西暦を入力してください:2000
#西暦2000年は、うるう年です 
#2020　※4*500+4*5,
#現在の西暦を入力してください:2020
#西暦2020年は、うるう年です 
#2200　※100*22or4*550,
#現在の西暦を入力してください:2020
#西暦2020年は、うるう年です
#2025　※どれでもない
#現在の西暦を入力してください:2025
#西暦2025年は、うるう年ではありません
'''
先生の解答
def is_leapyear(year):
    return (year % 400 == 0 or (year % 4 == 0 and year % 100 != 0))
    
current_year = int(input("現在の西暦を入力してください："))
if is_leapyear(current_year):
    print(f"西暦{current_year}年はうるう年です")
else:
    print(f"西暦{current_year}年はうるう年では」ありません")

'''

#%%　練習5－3
'''
次のコードについて、走った後は必ず歩くようにするために修正すべき点を挙げなさい。
ただし、関数ではない部分は変更しないこと。
def take_bus():
	print(‘バスに乗ります’)
def run():
	print(‘走ります’)
def walk():
	print(‘ちょっと歩きます’)
print(‘行ってきます!’)
walk();
take_bus();run();run();
print(‘ただいま’)
'''
def take_bus():
    print('バスに乗ります')
def run():
    print('走ります')
    walk()
def walk():
    print('ちょっと歩きます')
print('行ってきます!')
walk();
take_bus();run();run();
print('ただいま')

#Pythonという言語の根幹をなす**「実行モデル」と「動的な名前解決」**という仕組み
#def はただの「設計図の登録作業」である
#   defの時の関数定義は関数本体を実行しません; 
#   関数本体は関数が呼び出された時にのみ実行されます
#名前の解決は「ボタンが押された瞬間」に行われる
#   自由変数の名前解決はコンパイル時でなく実行時に行われます。」
#   つまり、名前が実際に存在するかどうか（エラーにするべきかどうか）を探しに行くのは、
#   定義した時ではなく、**「工場の稼働スイッチ（ () ）が押された瞬間」**なのです。
#練習問題のrun() の実行例:
#   一番下の行でついに run 工場の稼働スイッチが押されます。
#   run 工場が動き出し、「『走ります』と画面に出せ！」という命令を実行します。
#   次に「 walk() を呼び出せ！」という命令が来ます。
#   ここが 運命の分かれ道 システムはここで初めて周囲を見渡し
#   、「walk という工場はあるかな？」と探します。
#   すると、ずっと上の行（6行目）で def walk(): として工場がすでに
#   建築（定義）されていたため、システムは「おっ、ちゃんとあるな！よし、動かそう！」と判断し
#   無事に実行できるのです。
#   もし仮に、一番下の run() を呼び出す行よりもさらに下に def walk(): が
#   書かれていた場合（つまり、呼び出し時点でまだ設計図が登録されていない場合）
#   は、システムが探しても見つからず、NameError を出してクラッシュします。
#まとめPython（インタプリタ言語）: 
#   コードを読む段階では「とりあえず名前だけメモ」しておき、
#   「実際にその行が実行される瞬間（稼働時）」に部品が存在していればOK
#   とする（動的な名前解決）。
#%%　練習5－4
"""
試験の点数を分析する関数があります
def analze_scores(sansu,kougo,rika,syakai,eigo=None,*others):
	#処理内容は省略
	return [max_score,min_score,avg_score]
この関数について述べた1.～5.の文について、正しいモノは〇、誤っているものには、
✖を答えよ。また✖としたものは、その理由も説明せよ
"""
#設計図には、**3種類の異なる受け入れ口（引数）**が設定されています。
#必須引数 (sansu, kougo, rika, syakai) 
#   デフォルト値が設定されていないため、絶対に渡さないとエラーになる引数です。
#デフォルト（オプション）引数 (eigo=None) 
#   最初から「None（空っぽのカプセル）」がセットされている引数です。
#   もし外部から材料が渡されなければ、勝手に None を使って稼働してくれます
#。 ⇒pythonでは変数自体を式に組み込むときは,代入演算子を使って、
#   eigo:=None,のようにしないと駄目かと思っていた。
#   関数の仮引数として予めeigo=Noneの様に値を設定しておくのは何故OK？
#   またこれは文法としてはどんなものに分類されるの？
# A．def のカッコの中で使われる = は代入演算子（式）ではなく、
#   **関数定義という専用の看板を描くための「特殊な構文ルール（デフォルト引数値）」 
#   「ウォルラス演算子（:=）」は、**「式（計算の途中）」の中で無理やり代入を行って
#   値を取り出すための特殊なドリルでした
#   関数定義専用の特別なルール（構文）であるため、通常の代入を意味する 
#   := を使う必要はない。
#可変長引数 (*others) 名前の前に *（アスタリスク）が
#   ついている引数は、「あふれた材料を全てまとめて1つのタプル（変更不可能なカプセル）
#   に入れて受け取る」という特殊なゴミ袋のような役割を果たします
#   。何もあふれなければ、空のタプル () になります。
#⇒　kwargue?みたいな奴か？
#   アンパック演算子の*とはどのように見分けるの？
#A. *others の正体と、*（アンパック）の見分け方
#① 工場を作る時（def の中）の * ＝ 「パッキング（まとめるゴミ袋）」
#*others（通称 *args / 可変長位置引数）: 
#   名前を付けられずに放り込まれたあふれた材料を、
#   すべて 「タプル」 にまとめて受け取ります
#**kwargs（可変長キーワード引数）:
#   名前=値 の形で放り込まれてあふれた材料を、すべて
#    「辞書（ディクショナリ）」 にまとめて受け取ります
#⇒この時の値と対になる キー には何が設定されるの？
#A.**kwargs の「キー」には何が設定されるのか？
#   　【結論】 関数を呼び出すときに指定した「変数名（引数名）」の文字列が、
#   そのまま辞書のキーになります。
#   例：算数〜社会は必須の箱に収まる。eigoも80で上書きされる。
#    しかし、taiiku と ongaku という箱（仮引数）は設計図にない！
#   analyze_scores(sansu=80, kougo=90, rika=70, syakai=80,
#    eigo=80, taiiku=100, ongaku=90)
#   このとき、システムは「おっと、taiiku と ongaku という材料があふれてしまったぞ。
#   よし、これを **kwargs（可変長キーワード引数）のゴミ袋に入れよう」と判断します。 
#   その際、システムは**「渡された時の変数名」を文字列（キー）にし、「渡された値」を
#   バリュー**として、以下のような辞書を自動生成して関数内部に渡します
#    関数内部の kwargs の中身
#   {'taiiku': 100, 'ongaku': 90}
#② 工場を動かす時（呼び出し時）の * ＝ 「アンパッキング（展開）」
#   関数を呼び出すときに analyze_scores(*my_list) のように
#    * をつけると、今度は逆に「箱（リストなど）に入っている材料を、
#   バラバラに展開して（アンパックして） ベルトコンベアに流し込む」という正反対の動きをします
#　【見分け方のまとめ】
#def の中の * ➔ 散らばった材料を箱に**「詰める」**。
#呼び出す時の * ➔ 箱に入った材料を**「バラバラにぶちまける」**。

#　【徹底解剖】 Python内部での * の「3つの顔」
#   公式言語リファレンスに基づき、Pythonのシステム内部で * がどのように定義
#   （使い分け）されているか、その全容をマップ化します。
#① 式（計算）のコンテキストにおける * ＝ 【乗算・反復】
#   数値 や 変数 の 「間」 に置かれた場合、システムはこれを「二項算術演算子」
#   として評価します
#   ex:左辺と右辺が数値の場合: 単純な「掛け算」をします。（例：2 * 3 ➔ 6）
#   ex:一方がリストや文字列の場合: システムは気を利かせて
#   「シーケンスの繰り返し（Repetition）」を行います。（例：* 3 ➔ ？）
#② 関数のコンテキストにおける * ＝ 【パッキング / アンパッキング】
#   関数のカッコ () の中で使われた場合、システムは「これは計算の掛け算ではなく、
#   引数の特殊な操作だ！」とモードを切り替えます。
#   定義時（def func(*args):）: 余った位置引数をすべて包み込んで、
#       1つの**「タプル」**に圧縮（パッキング）します
#   呼出時（func(*my_list)）: 
#        逆に、リストやタプルなどのイテラブルオブジェクトの要素を、空中にパラパラと
#       　展開（アンパッキング）して、複数の引数として工場に流し込みます
#③ 代入のコンテキストにおける * ＝ 【星付きターゲット（拡張アンパック代入）】
#   代入文（=）の左側で使われた場合、システムはこれを「代入先の
#   特殊な受け皿（星付きターゲット）」として認識します
#    aには1が、cには4が入り、残った [4, 8] がすべて b という「リスト」にパッキングされる！
#   a, *b, c = [3, 4, 8, 9] 
#   この機能はインフラ実務でのログ解析（「最初のIPアドレスと、最後のステータスコードだけ
#   変数に入れて、真ん中の可変長なメッセージは全部リストにまとめておきたい」といった
#   処理）で狂ったように多用されます。
"""
この関数を呼び出すときに、少なくとも算数、国語、理科、社会、英語の5教科の点数を
引数として与えなければならない
⇒✖eigo=Noneにしているのでeigoの値を渡さなくても動く？
    *othersの*は省略可能を表している？

算数、国語、理科、社会、英語がすべて80点、音楽と体育と美術がすべて70点の場合、
この関数は次のコードで呼び出せる
analyze_scores(80,80,80,80,80,70,70,70)
⇒〇？受け取ったeigoはすぐNoneを指すようにされてしまう？

次のコードでこの関数を呼び出したとき、仮引き数otherにはint型の80ではなく、
要素数１のリスト[80]が渡される
analyze_scores(80,80,80,80,80,80)
⇒✖？

次のコードでこの関数を呼び出すと、仮引き数の定義とは異なる任意の順で    
       実引数を指定できる
analyze_scores(eigo=80,kokugo=20,rika=30,syakai=40,sansu=70)
⇒？
"""
#eigo=80 のように「変数名＝値」の形で材料を渡す方法を 「キーワード引数」 と呼びます
#この書き方をすると、システムは「なるほど、この値は eigo という名前の箱に
#入れればいいんだな」と名札を見て判断してくれるため、引数を渡す順番を完全に
#無視して自由な順番で書くことができるようになります。
#インフラのAPI操作などで引数が多い場合に超頻出の書き方です。
"""
5.　この関数は次の２つの方法で呼び出せる
	result = analyze_scores(1,2,3,4,5)
or
	[x,n,g] = analyze_scores(1,2,3,4,5)
⇒？
"""
# 1つ目の result = ... は、
#    出荷されたリストの箱をそのまま1つの変数に受け取る方法です。 
# 2つ目の [x,n,g] = ... は、
#    リストやタプルから複数の要素を同時に取り出して別々の変数に割り当てる
#    「アンパック代入（シーケンスのアンパック）」 と呼ばれる強力な機能です
#   出荷されたリストの中身（3つ）と、受け止める変数の数（3つ）が一致しているため、
#   自動的に分解されて代入されます。
#⇒ 受け止める変数の数が一致していない場合はどんな動作になるの？
#A. 【エラー発生！】受け止める箱が2つしかない
#   [x, n] = result  
#   ➔ ValueError: too many values to unpack (expected 2)
#    （多すぎて受け止めきれない！）
#   その場合、受け止める側の変数に *（星付きターゲット）を付けると、
#   あふれた分を自動的にリストにまとめて受け止めてくれるという強力な
#   回避テクニック（拡張アンパック代入）が存在します
#   例：先頭だけ x に入れ、残りはすべて others_list というリストに詰め込む
#   [x, *others_list] = [9-11, 13, 14]
# 「リスト」と「タプル」の根本的な役割（意味論）の違い
#   ここが最もアーキテクト的な視点です。公式ドキュメントでは、この2つのコンテナの
#   「実務での使われ方の違い」を以下のように明確に定義しています
# リスト（可変）の設計思想：**「同じ型」**のオブジェクトをたくさん並べて、
#   for ループで順番に処理（イテレート）するための列。
# タプル（不変）の設計思想：**「異なる型」**のオブジェクトをひとまとめにするためのもの。
#   インデックス（0番目、1番目…）の 場所そのもの に意味があるレコード
#   （C言語の構造体のような役割）。
#Pythonがデフォルトでタプルを生成する理由
#   安全だから： 受け渡しの途中でデータが改ざんされない（ラミネート加工の保護）。
#   速いから： メモリを余分に確保せず、一瞬で生成できる。
#   意味が正しいから：
#        カンマ区切りのデータは「イテレートするための列」ではなく
#        、「ひとまとめのレコード（C言語の構造体）」として扱われるべきだから

#%%　練習5－5
'''
次のようなルールに基づいて割り勘を計算するプログラムがある
・１人あたりの支払い額は支払総額を参加人数で割った金額
・支払いの単位は100円とし、100円未満の金額がある場合は切り上げる
・支払額を超過した分は、幹事が受け取る
#計算データの入力
amount = int(input(‘支払総額を入力してください：’))
people = int(input(‘参加人数を入力してください：’))

#割り勘の計算
dnum = amount / people	#総額を人数で割る（端数も保持）
pay = dnum // 100 * 100	#100円未満を切り捨て
if dnum > pay:			#元の値と比較し、
	pay = int(pay + 100)	#小さければ100円未満があったで上乗せ
#幹事の支払い額の計算
payorg = amount - pay *(people  - 1)

#結果の表示
print(‘支払い額’)
print(f’1人あたり{pay}円({people}任）、幹事は{payorg}円です’)

次の1.～3.の機能について、それぞれの仕様に従って関数に部品化して利用するプログラムに修正せよ
int_input関数
機能：画面に入力を促すメッセージを表示し、入力結果を数値に変換して返す
例、～～を入力してください：
引数：入力を促す項目を示す文字列
戻り値：入力された数値
calc_payment関数
割り勘の額を計算する。ただし、幹事以外の支払額は100円単位に丸めて切り上げ
例、234 →300、1403→1500
引数：支払総額、参加人数（省略時は２）
戻り値： 1人当たりの支払額（100円単位）と幹事支払額
show_payment関数
渡された引数を見やすく表示する
例、1人当たり～円（～人）、幹事は～です
引数：支払額、幹事支払額、参加人数（省略時は２）
戻り値：なし
'''
def int_input(msg): #表示文字を受け取り、入力結果を数値に変換して戻す
    return int(input(f"{msg}を入力してください："))
#Cでもreturnで式を返せるが、pythonでreturnで式を返す時の動作について↓
#Pythonにおいて「式（Expression）」とは、計算や評価を行って必ず何らかのオブジェクト（値）を
#生み出すものと定義されています
#例えば year % 400 == 0 という式が評価されると、システムはメモリ上のどこかにポツンと
# True または False という「真偽値カプセル」を生み出します。
#return を使うと、式リストを戻り値として、現在の関数呼び出しから抜け出します」**と明記されています
#つまり、return の仕事は「右側に書かれた式を計算させ、ポンッと生まれた出来立てのオブジェクトを
#掴んで、そのまま外に放り投げる」ことだけなのです。
#どうせすぐに剥がす名札なら、最初から貼らなくていいじゃないか。
#型検査のルールもないのだから、計算して生まれた名無しのオブジェクトを、そのまま即座に外に投げて
#（returnして）しまえ！」
#これが、**「計算しながら即座に出荷（リターン）するというショートカット」**の正体です。

#元の書き方：計算データの入力
#amount = int(input(‘支払総額を入力してください：’))
#people = int(input(‘参加人数を入力してください：’))
#DRY原則(Don't Repeat Yourself)上記のコードはほぼ同じ形で作られている。
#共通している部分を抜き出す。
#   int(input('〇〇を入力してください：'))
#違う部分〇〇だけを外から材料として放り込める関数を作れば、一つに処理をまとめ
#られるのでは？と考える。
#仮引数がstrと書かれている訳でないのに、
#msgと書くだけで仮引数はなぜ大丈夫なの？
#    【結論】 Pythonでは、変数はデータを入れる「箱」ではなく、
#    データに貼り付ける「名札（ポインタ）」だからです。
#msg というのはただの空っぽの「名札」です。 外部から "支払い金額" という
#「文字列オブジェクト（実体）」がコンベアで流れてきたら、システムはその実体にポンッと
# msg という名札を貼り付けるだけなのです。だから事前の型指定は不要です。
#もし「文字列以外が来たら困る」という設計者の意図を残したい場合は、以前
#少し触れた 型ヒント（Type Hint） を使って以下のように書くことができます。
## msg: str は「材料として文字列(str)を期待している」というメモ
# -> int は「完成品として整数(int)を出荷する」というメモ
#def int_input(msg: str) -> int:
    #return int(input(f"{msg}を入力してください："))
#
#return amountとしては駄目なのはなぜ？
#return　計算式という書き方が全然思いつかなかったので、不安になっているのかな。
#defのスコープの中でreturn amount
#と記述した後に、スコープ害でamount = int_input("支払金額")
#と書くのは混乱の元になる？この場合はdef内の仮引数名と、スコープ外の
#Q.変数名をどうするのがベストプラクティス？
#A. 【関数の中】汎用的な名前（user_inputやresultなど）を使う
#def int_input(msg: str) -> int:
#    user_input = int(input(f"{msg}を入力してください："))
#    return user_input
# 【関数の外】具体的な名前（amountやpeopleなど）を使う
#amount = int_input("支払総額")
#people = int_input("参加人数")
amount = int_input("支払い金額")
people = int_input("参加人数")
def calc_payment(amount, people=2):
    #Q.仮引数に日本語を使うのはあり？
    #A.文字化けなどの危険あり**変数名や関数名は「必ず半角英数字の
    #英語（ローマ字）」で書くのがプロの絶対的な鉄則（コーディング規約）**です。
    #割り勘の計算
    dnum = amount / people	#総額を人数で割る（端数も保持）
    pay = dnum // 100 * 100	#100円未満を切り捨て
    if dnum > pay:			#元の値と比較し、
    	pay = pay + 100#小さければ100円未満があったで上乗せ
    #幹事の支払い額の計算
    payorg = amount - pay *(people  - 1)
    return [int(pay), int(payorg)]
    #Q.この最後の[]内の書き方はどんな意図と文法になっている？
    #関数（工場）の絶対ルールとして、**「1回の return で出荷できる製品
    #（オブジェクト）は1つだけ」**という原則があります。しかし今回は2つの値を返したい。
    #そこで、2つのデータを [ ] で囲み、1つのリストというコンテナ（箱）にパッキングして
    #出荷しているのです。
    #[]だとタプルではなくリストになるのはなぜ？
    #return int(pay), int(payorg) （※これだと不変の ( ) 
    #タプルとして出荷される） ←これはなぜ？
    #Q. なぜ [] はリストで、 , はタプルになるのか？
    #A.公式ドキュメントには、「リストは、角括弧の中にカンマで区切られた式を並べて作ります
    #」と定義されています
    #パーサは [ を見つけた瞬間、「おっ、これから来るのは**リスト（後から変更可能な
    #バインダー）**だな」と解釈し、専用のリスト構築エンジンを起動させます
    #A.公式ドキュメントの絶対ルールに**「リスト表示や辞書表示の一部になっているものを
    #除き、少なくとも一つの カンマ を含む式の リスト は タプル になります」と明記
    #されています
    #つまり、全体を囲む [ などの記号がない状態でカンマ , が現れると、
    #システムは「なるほど、これはタプル（変更不可能なラミネート加工のカプセル）**への
    #パッキング（梱包）だな」と自動的に判断するのです

[pay,payorg] = calc_payment(amount,people)
def show_payment(pay,payorg):
    #結果の表示
    print('支払い額')
    print(f'1人あたり{pay}円({people}任）、幹事は{payorg}円です')
show_payment(pay, payorg)






