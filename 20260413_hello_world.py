# -*- coding: utf-8 -*-
"""
Spyderエディタ

これは一時的なスクリプトファイルです。
"""
###1.5.3[print]
print("Hello World!!")
#Pythonでは//は除算切り捨てに使われているので#をコメントに使う。
name ="山田"
num = 3
print(name)
print(2*5)#計算結果が表示される。
print("Hello",name ,'さん！') 
 #,で区切ると複数の式・変数を表示できる。
 #Hello 山田 さん！,が空白になる。
print("Hello",name,'さん！',sep="+")
#Hello+山田+さん！
print("hello",num,'さん！',sep="+")
#hello+3+さん！
#print("hello" + num +'さん！')
#TypeError: can only concatenate str (not "int") to str
#これはエラーになる。
print("hello"+ str(num) +'さん！')
#hello3さん！
print(int("10")+num)
#13
print('こんにちは',end=" ")
print('こんばんは')
#こんにちは こんばんは
#sep や end は予約語ではない。
#これらprint 関数にあらかじめオプションとして用意されている「キーワード引数」
#デフォルトではseparatorには空白、endには\nが設定されている。

###1.5.4[大文字/小文字][コメント]
"""
疑似的に複数行コメント
ただし基本は#。""""""は入れ子にすることが出来ない。
"""
"""#"""#"""#"""
'''
厳密にはpythonには複数行コメントという機能は無い。
'''
"""」これの本来の重要な役割は（ドキュメンテーション文字列） 
コンパイラによって認識され、対象となるクラス、関数、モジュールの __doc__ 属性に入れられる"""
#正式な使い方
def setup_router():
    #Pythonの世界では全てがオブジェクト。def setup_router():と記述した瞬間、
    #システムは単なる処理の手順書を作る以外に、それ自体を包み込んだ
    #[ユーザ定義関数オブジェクト］という実体を作り出す。
    #obj.nameの書き方は[属性参照]と呼ばれる。
    #[__doc__]は関数オブジェクトにシステムが予め用意している[読み取り専用特殊属性（付属データ）]
    """
    この関数はルーターの初期設定を行います。
    引数：なし
    戻り値：設定が成功したかどうかの結果(True/False)
    """
    # ここに実際の処理（真のコメント）
    pass

# ---------------------------------------------------------
# アプローチ1：システムが記憶した __doc__ 属性を直接覗き見る
# ---------------------------------------------------------
print("▼ __doc__ 属性を直接出力した結果")
print(setup_router.__doc__)

# ---------------------------------------------------------
# アプローチ2：組み込みの help() 関数を使って、
# 公式マニュアルのように整形して出力させる（実務でよく使います）
# ---------------------------------------------------------
print("▼ help() 関数を使って出力した結果")
help(setup_router)
'''
▼ __doc__ 属性を直接出力した結果

    この関数はルーターの初期設定を行います。
    引数：なし
    戻り値：設定が成功したかどうかの結果(True/False)
    
▼ help() 関数を使って出力した結果
Help on function setup_router in module __main__:

setup_router()
    この関数はルーターの初期設定を行います。
    引数：なし
    戻り値：設定が成功したかどうかの結果(True/False)
'''



