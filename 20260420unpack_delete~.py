# -*- coding: utf-8 -*-
"""
Created on Mon Apr 20 12:52:44 2026

@author: iot01
"""
##3.2.4アンパック代入続きから
data = [1,2,3,4,5,6,]
a,_,b,_,_,c = data
#何も指定しない代入は位置指定代入？なので代入するコンテナ要素の中身の順番に
#代入される。同じ変数に代入された場合は最後の要素で上書きされる。
print(a)#1
print(b)#3
print(c)#6
print(_)#5
    #[_]はそもそも普通の変数。 なので最後の値で上書きも[*]も使える。
a,*_,b,c = data
print(_)
#_ = [2, 3, 4]
r'''
*は、「"星付き"のターゲット（starred target）」と呼ばれている。
ので目標とする対象の前に着けてターゲットにするイメージ。
通常のアンパック代入（例：a, b, c =
）は、左の箱の数と右のデータ数が完全に一致していないとエラー（ValueError）を起こします。
*の後ろにある変数には 後ろの変数から順番に要素の後ろの値が 代入される。
_ （アンダースコア）はただの変数だが、習慣的にこの変数に入ったデータは後で使わない、
と言う意図を伝える為のごみ箱変数の名前として良く使われる
'''

data = [1,2,[31,32,33]]
#pythonはリスト≒配列に異なるデータ型のものが入っても良い。
#なぜならリストの中身はただの参照先だから。
#名札のサイズ自体はどれも同じ（64bitのメモリアドレス）
a,b,c, = data
print(a)#1
print(b)#2
print(c)#[31, 32, 33]
a,b,(z1,z2,z3) = data
#コンテナの3番目の要素がまたコンテナで、アンパックで代入したい時は、
#（）変数を括弧で括る。再帰的なアンパック代入と言われる。
# 左側の受け皿の形（構造）が右側のデータ構造と一致しているから、
#もう一段階深く潜って（再帰的に）アンパックしてやるぞ！
# z1 に 31、z2 に 32、z3 に 33 を代入して完了
print(z1,z2,z3)#31 32 33
#printはデフォルトでsep=' 'になっている。

##！！変数のスワッピング
x = 15
y = 38
x,y = y,x
print(x,y)#38, 15
R'''
C言語の様に変数の中身を入れ替えるのに3つ目の変数を用意する必要はない。
変数に入っているのはオブジェクトへの参照先。
Q.x,y = y,x これ式の左側から評価がされるのであれば、
  x=38,y=38となって参照先のスワップが上手くできなそうだけれど、
  内部的にはどんな順序と仕組みでスワップが行われている？
A.Pythonの公式ドキュメント（6.16. 評価順序）
「Python は、式を左から右へと順に評価します。
ただし、代入式を評価するときは、右辺が左辺よりも先に評価されます。」
x,y=y,xの裏側、タプルパッキング：
代入だ！まず右辺の評価をする参照先は38,15。
「よし、カンマで区切られているから、これはタプルだな！
 裏側で (38, 15) という『一時的なタプルオブジェクト（荷物置き場）』
 をメモリ上に作って、そこに参照を避難させろ！
 そして左辺評価時に避難タプルオブジェクトから参照が渡されるので、問題ない。
 つまりpythonでは 変数temp を自動的に作ってくれている。
 公式ドキュメントでは左辺と右辺へのオーバーラップは同時と書かれている。
'''

#3.2.5新しい代入演算子[:=]セイウチ演算子(walrus)
#y = (X = 20) / 10
#SyntaxError: invalid syntax. Maybe you meant '==' or ':=' instead of '='?
#ちなみに y = (x == 20) /10 とすると、
#NameError: name 'x' is not defined. Did you mean: 'Xとなる。
y = (X := 20) / 10
print(y)

##3.3比較演算子
#Pythonでは比較式を作ると評価が行われてTrueかFalseが返ってくる。
#↑これはなんでこんな仕様になったの？if文とかを省く為？
#異なる方同士での比較
print(1 == '1')
    #False：int型とstr型
print(False == None)
    #False：bool型とNoneType型？
print(1 == 1.0)
    #True
print(1.5 < 1)
    #False
print(15 < 131)
    #True
print('15' < '131')
    #False
print(1000 is 1000)
    #True
print(1 is 1)
    #True
a = [1,2,3,]
b = [1,2,3,]
print(a[1],b[1]) #2 2
print(a[1] == b[1]) #True
print(a[1] is b[1]) #True
print(id(a[1]),id(b[1])) #140705391846216 140705391846216
print(id(a[1]) == id(b[1])) #True
print(id(a[1]) is id(b[1])) #False
#id()実行瞬間に15桁の整数オブジェクトが2つ作られているので2つは違うオブジェクト
print(id(id(a[1])) is id(id(b[1])),"\n") #False
'''
 print(id(a) is id(b)) の裏側で何が起きたか
① まず aと bの関係（おさらい） 
   aの中身は 2 、 bの中身も 2 です。
   「2」はシステムが最初から用意して使い回している「小さな整数（-5〜256）」なので、
  実は a も bも、メモリ上の全く同じ「2」の箱を指しています。
② id() 関数が実行される id(a) を実行すると、システムは「2」の箱の住所（メモリアドレス）を調べます。
　これは例えば 140722332243752 のようなものすごく大きな数字になります。
③ システムが「新しい箱」を作る システムは、調べ出した 140722332243752 という
　大きな数字を画面に表示したり比較したりするために、この大きな数字を入れるための
　 「新しい整数の箱」をメモリ上に新規作成します。
  同じように、右辺の id(b) を実行したときにも、値自体は同じ 140722332243752 ですが、
  システムはそれを入れるためのもう一つの「新しい整数の箱」を別で新規作成します。
④ 比較演算子の出番
  id(a) == id(b) の場合：
  「中身の数字は同じか？」と聞かれているので、両方とも 140722332243752 だから True を返します。
id(a) is id(b) の場合：
 「右と左は、全く同じ箱か？」と聞かれています。中身の数字は同じですが、
 ③の通り、システムが関数を実行した都度、別々の「大きな数字の箱」を作ってしまっているため、
 箱としては別物と判定され False になります。
'''

a = 1000
b = 1000
print(a is b,'\n')
    #True
print(1000 is 1000)
print(1 is 1)
'''
d:\python\20260420unpack_delete~.py:45:
SyntaxWarning: "is" with a literal. Did you mean "=="?
  print(1000 is 1000)
d:\python\20260420unpack_delete~.py:46: 
SyntaxWarning: "is" with a literal. Did you mean "=="?
  print(1 is 1)
  「with a literal」は「リテラル（ベタ書きのデータ）と一緒に
  （is 演算子を使っているぞ）」 という意味。
  「リテラル（literal）」とは、変数を使わずに
  「ソースコード上に直接ベタ書きされた値（定数）」のこと。
  print(a is b) で警告が出なかったのは、「変数（名札）」を通した比較であったため、
  現場監督のチェック（リテラルへの直接使用の検知）をすり抜けたから。
  警告の意味 ＝ 「リテラルに対して is（メモリ住所の比較）を使うのは挙動が不安定で危険だから、
  値の比較 == を使いなさい」というシステムからの親切なアドバイス。
'''

#3.3.2リストの比較
data1 = [1,2,3]
data2 = [1,5]
data3 = [1,2]
data4 = 'a'
data5 = 'b'
print(data1 < data2)#True最初は 1 == 1 で引き分け。次は 2 < 5 後ろの要素数に関係なくここで True
print(data1 < data3,'\n')#False1==1、2==2 と進み、右側の弾が尽きました。
#完全に一致したまま短い方が先に弾切れになった場合のみ、短い方を小さいと見なす」というルール
print(data4 > data5)#False
R'''
要素数が少ない方が小さいと見なされる。
文字はアルファベット順が後ろの方が大きいと評価される。
Q.他に覚えておいた方が良いデータ型の比較結果はある？
A.
１．リストとタプルなど、異なるシーケンス型同士を比較しようとすると、
等価比較 (==) は常に False となり、大小比較 (< など) を行うと
TypeError （比べられないぞ！）が送出されます
例えば  == (1, 2) は False です。
2.辞書型は、キーと値のペアが完全に一致しているかを判定する == と != はサポートしていますが、
大小比較（< や >）を行おうとすると TypeError 。
3.集合（set）の < は「部分集合」を意味する。
A < B は「Aの要素数がBより少ないか」ではなく、
「AがBの『真部分集合』であるか（Aのすべての要素がBに。含まれており、かつAとBが等しくないか）」
を判定します 。
4.【罠】NaN（非数 Not a Number）は「自分自身とも等しくない」狂気の世界
IEEE 754という浮動小数点数の国際規格に従い、NaN は自分自身を含め、
いかなる値とも等しくない（NaN == NaN は False、NaN != NaN は True）という
直感に反する挙動をします。
NaNの判定には == ではなく、専用の math.isnan() を使う必要がある。
'''

#3.3.3浮動小数点の比較
print(0.2*3 == 0.6)
    #浮動小数点は内部的には1/2^nの足し算で近似している為Trueにならない。
#では？浮動小数点を比較するには？？
#-1：Decimal型を使う
#-2：[丸め]単位による比較
'''
「 ϵ−δ （イプシロン・デルタ）論法」で「誤差（Error）」や
「差異（Écart：フランス語）」の頭文字である『 e 』に相当するギリシャ文字
『 ϵ （イプシロン）』**が記号として採用されました。
これが「イプシロン＝限りなくゼロに近い微小な数」というイメージの起源です。

計算機科学者たちは、そのコンピュータ（や型）が認識できる、 1 より大きい最小の数 と 1 との差」
のことを、数学の慣習に倣って 「マシン・イプシロン（Machine Epsilon）」 と名付けました。
 「このシステムにおける限界の誤差」という意味合いから、
 プログラミングの世界でも
 許容誤差を定義する定数名として EPSILON を使うことが
 世界共通の暗黙の了解（ベストプラクティス）となっているのです。
'''
EPSILON = 0.00001 
    #許容誤差を1/10^5と設定
x = 0.2*3
y = 0.6
print(abs(x-y))#1.1102230246251565e-16
print(abs(x-y) < EPSILON)
    #true
#-3：isclose関数の使用
import math
    #mathモジュールをインポートすることで使用可能になる。
print(math.isclose(0.2*3, 0.6))
    #True
'''
isclose()は厳密に等しくなくても、近似であればTrueを返す（小数点以下9桁まで）。
'''
print(math.isclose(0.1, 0.1001, rel_tol=0.0001))#False
print(math.isclose(0.1, 0.1001, rel_tol=0.001),'\n')#True
'''
** 関数呼び出し時の「名前=値」の正体（キーワード引数）
- 引数の中の `=` は、Pythonでは代入（:=）ではなく「キーワード引数」という特別な構文。
- C言語のように「引数の順番と意味を暗記する」苦痛を終わらせるための魔法。
- メリット①：順番を完全に無視して渡せる（システムが名前を見て自動で割り当てる）。
- メリット②：コード自体が「説明書」になり、後から読んでも意味が一目でわかる（可読性の向上）。
    math.isclose関数の定義の中であらかじめキーワード引数として設定されているもの。
    rel_tol は（相対許容誤差）の意味。
▲補足1：値がゼロ（0.0）と比較される場合の致命的な罠
rel_tol は「2つの値のうち大きい方」を基準にしてパーセンテージで誤差を許容します。
なのでもし math.isclose(x, 0.0) のように「ゼロ」と比較しようとした場合、
計算式は abs(x) <= rel_tol * abs(x) となり、どんなに x がゼロに近くても
（x がゼロそのものでない限り）絶対に False になってしまいます。
ゼロとの比較（あるいはゼロに極めて近い値の比較）を行う場合は、
割合ではなく「絶対許容誤差」である abs_tol を明示的に指定する必要があります。

▲補足2：IEEE 754 がもたらす NaN（非数）と inf（無限大）の挙動
ネットワーク機器のセンサーデータ（温度やトラフィック）を扱う際、
データが欠損していて float('NaN')（Not a Number）や math.inf（無限大）という
特殊な浮動小数点数が混ざることがあります。
自分自身（NaN == NaN）ですら False になるのはmath.isclose() でも同様。
一方で、inf と -inf については、自身とのみ「近い」と見なされる（Trueになる）という、
人間からすると少し直感に反する厳格なルール。

▲補足3:【アーキテクチャ】 Decimal 型と fractions（有理数）の分業
Pythonは「どうしても正確な計算が必要なレアケース」のために
、2つの強力な標準ライブラリ（別動隊）を用意しています。この2つは目的が明確に分かれています。
decimal モジュール (10進数)
　用途: お金（財務）の計算や、人間が手計算した結果と完全に同じ結果」を求められるアプリケーション用
　特徴: 2進数の近似による誤差を排除し、有効桁数を人間が指定した通りに厳密に管理します。
fractions モジュール (有理数 / 分数)
　用途: 1/3 のような、「10進数でも無限小数になってしまう数」を完全に正確に表現したい場合。
　特徴: 内部的に「分子と分母」のペアとしてデータを保持するため、
  どれだけ計算を繰り返しても精度が一切劣化しません。
'''

#3.3.4同一性と同値性
data1 = [1,2,3]
data2 = [1,2,3]
print(id(data1),id(data2))
print(data1 == data2)
    #True
print(data1 is data2)
    #False
#同じ値のリストでも別のリストオブジェクトとして生成されているので、
#id≒参照先？を比較するis演算子では Falseになる。
    
#!!注意、イミュータブル型の同一性
data1 = 'あいう'
data2 = 'あいう'
print(data1 == data2)
    #True
print(data1 is data2)
    #True
hoge = 1.23
print(hoge == None)#False
print(hoge is None,'\n')#False
#Pythonにおいて None は「比較できない」のではなく、「世界に一つだけの『空っぽ』の箱」
#hoge は、日本のプログラミング界隈で頻繁に使われる 「メタ構文変数」 
#日本の定番： hoge（ホゲ）、 fuga（フガ）、 piyo（ピヨ）
#英語圏の定番： foo（フー）、 bar（バー）、 baz（バズ）
#数学で言うところの： x や 
'''
【備忘録：同値性と同一性の使い分け / キーワード引数の正体】
** 同値性(==)と同一性(is)の実務的な使い分け
- == (同値性): 「値（意味）が同じか」を比較する。日常的な比較の99%はコレ。
- is (同一性): 「メモリ上の箱が同じか」を比較する。
  ★超重要ルール (PEP 8)：実務で `is` を使うのは、原則として「None（空っぽ）かどうか」を
  判定する時だけ！
  （Noneはシステム上に1つしかない特別な箱なので、isで判定するのが最も安全で高速）
  == を使うと、システムは裏側でわざわざ __eq__() メソッドを呼び出すという
  「関数実行のコスト」を支払います。
  しかし is は、単純にメモリの番地（C言語でいうポインタのアドレス）を比較するだけ
'''

#3.3.5条件演算子（指定された条件式に応じて、対応する[式]の値を返す）
score = 75
print('合格' if score >= 70 else '不合格...')
    #　式１ if 条件式　else　式2
score = 55
print('合格' if score >= 70 else '惜しい、もうちょっと...' if score >= 50
      else 'もうちょっと頑張ろう')
    #Return A if condition is true, else return Bの様に英文として読めば語順は自然。
score = 55
if score >= 70:
    print('合格')
elif score >= 50:
#!!else if ではなく[elif]になっていることに注意
    print('惜しい、もうちょっと\n')
else:
    print('もっと頑張ろう')

##3.4論理演算
#3.4.1ショートカット演算（短絡演算）
'''
真偽を決定づけたオブジェクト（値）そのものをポンとそのまま投げ返す」
という非常にユニークな設計になっています
結果：
実行されました。
実行されました
True
1
False
True
4
'''
x = 1
if x != 2:
    print('実行されました。')
print(x == 2 or '実行されました')
x = 2
if x != 2:
    print('実行されました。')
print(x == 2 or '実行されました')
#ショートカット演算の実例
print(True and 1)
print(False and 2)
print(True or 3)
print(False or 4)

##3.5ビット演算子
#3.5.1ビット論理演算子
#ビットシフト演算子
r'''
【メンタルモデル】 ビット演算とは何か？
ビット演算とは、数値を「10進数」ではなく「2進数（0と1の並び）」の
ハードウェア・メモリレベルで直接操作する技術です。
普段、現場監督（システム）は「10」や「5」といった数値を人間向けに処理していますが、
ビット演算子を使うと、「裏側にある 1010 や 0101 といったスイッチのON/OFF」を
直接いじることができます。
Pythonでビット（2進数）を視覚的に確認するには、組み込み関数の bin() を使うと便利です。
--------------------------------------------------------------------------------
【論点の抽出1】 ビット論理演算子
2つの2進数の各桁（ビット）を見比べて、論理的なルールに従って新しい2進数を作る演算子です。
公式ドキュメントでは以下の4つが定義されています。
& (AND : 論理積) ➔ 両方が 1 のときだけ 1
| (OR : 論理和) ➔ どちらか片方でも 1 なら 1
^ (XOR : 排他的論理和) ➔ 互いに「違う」ときだけ 1
~ (NOT : ビット反転) ➔ 0 と 1 をすべてひっくり返す
'''
#■ サンプルコード1（ビット論理演算子）
# C言語の知識がそのまま活きるビット演算
x = 10  # 2進数: 1010
y = 12  # 2進数: 1100
print(f"xの2進数: {bin(x)}")#xの2進数: 0b1010
print(f"yの2進数: {bin(y)}")#yの2進数: 0b1100
print("-" * 20)#--------------------

# 1. AND (&) : 両方が1の桁だけ1になる（マスク処理でよく使う）
# 1010
# 1100
# ----
# 1000 (10進数で8)
print(f"x & y  = {x & y}  (2進数: {bin(x & y)})")#x & y  = 8  (2進数: 0b1000)

# 2. OR (|) : どちらかが1なら1になる
# 1010
# 1100
# ----
# 1110 (10進数で14)
print(f"x | y  = {x | y}  (2進数: {bin(x | y)})")#x | y  = 14  (2進数: 0b1110)

# 3. XOR (^) : 互いに違う桁だけ1になる
# 1010
# 1100
# ----
# 0110 (10進数で6)
print(f"x ^ y  = {x ^ y}  (2進数: {bin(x ^ y)})")#x ^ y  = 6  (2進数: 0b110)

# 4. NOT (~) : 0と1を反転（※Python特有の挙動あり）
# 10のビット反転は -(10 + 1) = -11 となる
print(f"~x     = {~x} (2進数: {bin(~x)})")#~x     = -11 (2進数: -0b1011)
r'''
※【アーキテクトの視点（罠）】Pythonの ~ (NOT) 演算子は、
少し直感に反する結果（10を反転すると-11）になります。
これは、Pythonがマイナスの数を表現するのに「2の補数表現」という仕組みを使っており、
公式ドキュメントでも ~x の計算結果は -(x+1) と定義されているからです。
--------------------------------------------------------------------------------
【論点の抽出2】 ビットシフト演算子
2進数の桁を、左や右にズラす（シフトさせる）演算子です。
<< (左シフト) ➔ 桁を左へズラし、空いた右側には 0 を詰めます。
>> (右シフト) ➔ 桁を右へズラし、あふれた右側は切り捨てます。
'''
#■ サンプルコード2（ビットシフト演算子）
x = 11  # 2進数: 1011
print(f"元のx: {x} (2進数: {bin(x)})")#元のx: 11 (2進数: 0b1011)

# 左へ1つシフト（<< 1）
# 1011 ➔ 10110 (10進数で22)
left = x << 1
print(f"x << 1: {left} (2進数: {bin(left)})")#x << 1: 22 (2進数: 0b10110)

# 右へ1つシフト（>> 1）
# 1011 ➔ 0101 (一番右の1が押し出されて消える。10進数で5)
right = x >> 1
print(f"x >> 1:  {right}  (2進数: {bin(right)})")#x >> 1:  5  (2進数: 0b101)
r'''
※【アーキテクトの真理】公式ドキュメントには、
n ビットの左シフトは pow(2, n) による乗算、右シフトは pow(2, n) による除算（切り捨て）
として定義される」と明記されています。
つまり、左に1つシフトすることは「値を2倍にすること」、右に1つシフトすることは
「値を1/2にすること（余り切り捨て）」と全く同じ意味です。
ハードウェアレベルでは、通常の掛け算・割り算よりもビットシフトの方が圧倒的に計算速度が速いため、
C言語の組み込み開発ではパフォーマンスチューニングの常套手段として使われます。
'''





